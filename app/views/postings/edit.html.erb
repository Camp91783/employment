<h1>Editing Posting</h1>

<%= render 'form' %>

<%= form_for @user, url: users_path, html: { multipart: true } do |form| %>
  <%= form.file_field :avatar %>
<% end %>

<%= link_to 'Show', @postings %> |
<%= link_to 'Back', postings_path %>


<p><a href="/">Back Home</a></p>